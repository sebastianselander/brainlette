use sort;

void test(int d) {
    printInt(d);
    sort::test(d+1);
}

int main() {
    test(0);

    
    int[] arr = new int[3];
    arr[2] = 0;
    arr[1] = 1;
    arr[0] = 2;


    sort::bubblesort(arr);
    for (int i : arr) {
        printInt(i);
    }

    return 0;
}

